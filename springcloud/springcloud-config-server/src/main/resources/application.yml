server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/709326140/test.git
          search-paths: test
          username:
          password:
      label: master
